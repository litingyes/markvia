---
title: Markvia
description: Universal Markdown Runtime for static, streaming and interactive content.
template: splash
prev: false
next: false
hero:
  title: Write once. Render everywhere.
  tagline: A universal Markdown runtime for static pages, streaming input, and interactive content.
  actions:
    - text: Open Playground
      link: /playground/
      variant: primary
    - text: Get started
      link: /getting-started/
      variant: secondary
---

import { Card, CardGrid } from '@astrojs/starlight/components'
import HtmlDemo from '../../components/demos/HtmlDemo.astro'
import { rendererSampleMarkdown as sampleMarkdown } from '../../components/demos/sampleContent'

<div class="markvia-landing__intro">

Markvia parses Markdown into a shared Semantic AST and Render IR, then adapts the same document to HTML, React, and Vue.

</div>

## One source, multiple targets

The lightweight preview below uses the same safe HTML renderer that powers static output. Use the Playground to edit the source and compare every target interactively.

<HtmlDemo content={sampleMarkdown} />

<CardGrid>
  <Card title="One semantic pipeline" icon="document">
    Parse once, keep stable node identity, and let each renderer consume the same Render IR.
  </Card>
  <Card title="Safe by default" icon="padlock">
    Text is escaped, unsafe URLs are rejected, and raw HTML stays inert unless a trusted HTML
    renderer opts in.
  </Card>
  <Card title="Built for change" icon="rocket">
    Stream incremental Markdown, transform documents, and add code-highlighting hooks without
    changing renderer contracts.
  </Card>
</CardGrid>

## Explore the runtime

Start with the [Getting Started guide](./getting-started/), inspect [renderer integration](./renderers/), or experiment with your own content in the [Playground](./playground/).
